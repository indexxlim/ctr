# Requirements for merlin.py
# Install using conda first for CUDA dependencies, then pip

# Core RAPIDS libraries (install via conda)
# conda install -c rapidsai -c conda-forge -c nvidia cudf=23.10 cupy=13.6

# Merlin libraries
nvtabular==23.08.00
merlin-core

# Machine Learning
xgboost>=2.0.0  # With GPU support

# Data processing
pandas>=1.5.0
numpy>=1.24.0
pyarrow>=12.0.0
dask>=2023.9.0

# Utils
scikit-learn>=1.3.0
psutil>=5.9.0
pynvml  # For GPU monitoring
